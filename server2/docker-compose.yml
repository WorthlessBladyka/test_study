services:
  redis:
    image: redis:7.4
    container_name: redis
    ports:
      - "6379:6379"
    networks:
      - identidock-net
networks:
  identidock-net:
    driver: bridge
    external: true 
